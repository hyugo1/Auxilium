//TODO:Use js for dynamically constructing footer, scrolling will done done via navbar

    // Add a smooth scrolling effect to the links in the footer
    /*
    $('footer a[href^="#"]').on('click', function(event) {
    var target = $(this.getAttribute('href'));
    if( target.length ) {
        event.preventDefault();
        $('html, body').stop().animate({
        scrollTop: target.offset().top
        }, 1000);
    }
    });*/
